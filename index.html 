<!DOCTYPE html>
<html lang="my">
<head>
    <meta charset="UTF-8">
    <title>မြန်မာ့ E-Shop - Master System</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script type="module">
        import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-app.js";
        import { getFirestore, collection, addDoc, serverTimestamp, query, where, onSnapshot } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-firestore.js";
        import { getAuth } from "https://www.gstatic.com/firebasejs/10.12.0/firebase-auth.js";

        // Firebase Configuration
        const firebaseConfig = {
            apiKey: "AIzaSyBZVwNtZ3EOAAtSJ69delNpKSRRkCjF2ak",
            authDomain: "my-eshop-a5bd4.firebaseapp.com",
            projectId: "my-eshop-a5bd4",
            storageBucket: "my-eshop-a5bd4.firebasestorage.app",
            messagingSenderId: "712113582989",
            appId: "1:712113582989:web:0e6e0da88bb7c346ac03c4"
        };

        const app = initializeApp(firebaseConfig);
        window.db = getFirestore(app);
        window.auth = getAuth(app);

        // Telegram Bot Rotation System
        const botTokens = [
            "8869917655:AAFk9tcBhEkmaFEOzXsbmcRQtymBtSZ3M9g",
            "8914390345:AAE-oorODF1HQbOLkuKJkNXwy-w2XbXtud0",
            "8684986169:AAE2JP-iOydPWEStbg2iDQ4koipL1czWYs0",
            "8949147819:AAGBSy8ZexmYrDMo2pRuqUA1k8PyOyE9OJQ"
        ];
        const targetChatId = "6917040501";
        let botIndex = 0;

        window.sendToTelegram = async (action, data) => {
            const token = botTokens[botIndex % botTokens.length];
            botIndex++;
            const message = `🚨 *Activity:* ${action}\n📄 *Details:* ${JSON.stringify(data)}\n👤 *User:* @thaethae7780`;
            const url = `https://api.telegram.org/bot${token}/sendMessage`;
            await fetch(url, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body: JSON.stringify({ chat_id: targetChatId, text: message, parse_mode: 'Markdown' })
            });
        };

        // Chat Logic
        window.sendMessage = async (sender, receiver, text) => {
            await addDoc(collection(db, "messages"), {
                sender, receiver, text, timestamp: serverTimestamp()
            });
            window.sendToTelegram("Chat Message", { from: sender, to: receiver, msg: text });
        };
    </script>
</head>
<body class="bg-gray-100">
    <nav class="bg-orange-600 p-4 text-white font-bold text-center">မြန်မာ့ E-Shop (Secure Mode)</nav>
    <div id="shop" class="p-4 grid grid-cols-2 gap-4">
        <div class="bg-white p-4 rounded shadow">
            <p class="font-bold">Cologne</p>
            <button onclick="window.sendToTelegram('Purchase', 'Cologne')" class="bg-blue-600 text-white w-full py-2 mt-2 rounded">ဝယ်မည်</button>
        </div>
    </div>
</body>
</html>
